# Public site URL (used for absolute URLs and OpenGraph)
NEXT_PUBLIC_SITE_URL=https://polaris.am

# Inbox where contact form submissions are delivered
CONTACT_EMAIL=hello@polaris.am

# Optional SMTP (left blank until nodemailer is wired in)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
